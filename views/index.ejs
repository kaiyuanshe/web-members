<% include partials/header.ejs %>
<div class="container">
    <div class="row">
      <div class="col-6 text-center">
        <a href="/add_volunteer"><h2>申请成为志愿者</h2></a>
      </div>
      <div class="col-6 text-center">
        <a href="/add_member"><h2>申请成为正式成员</h2></a>
      </div>
    </div>
</div>
    <div class="table-wrapper">
        <% if (players.length > 0) {%>
            <table class="table table-hovered">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">昵称</th>
                        <th scope="col">工作组</th>
                        <th scope="col">职位</th>
                        <th scope="col">当前状态</th>
                    </tr>
                </thead>
                <tbody>
                    <% players.forEach((player, index) => { %>
                        <tr>
                            <td scope="row"><%= player.id %></td>
                            <td><%= player.nick_name %></td>
                            <td><%= player.work_group %></td>
                            <td><%= player.position %></td>
                            <td><%= player.status %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-center">没有成员</p>
        <% } %>
    </div>
    <div class="text-center">
         <h2>加这个机器人为微信好友之后，才能通过申请加入开源社</h2>
         <img src="https://github.com/kaiyuanshe/wechat-robot/raw/master/logo.png"/>
    </div>
</div>
</body>
</html>
